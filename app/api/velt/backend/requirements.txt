Django==4.2.26
pymongo[tls,srv]==4.6.3
mongoengine==0.27.0
blinker==1.8.2
python-dateutil==2.8.2
requests>=2.25.0
django-cors-headers==4.3.1
boto3>=1.28.0
velt-py==0.1.14
gunicorn==21.2.0
python-dotenv==1.0.0
# resolver-endpoint-auth: PyJWT (+ cryptography for asymmetric algs) powers the
# built-in JWT verifier used by sdk.selfHosting.verifyToken (the velt-py[auth] extra).
# Pinned here so a plain `pip install -r requirements.txt` works.
PyJWT>=2.8.0
cryptography>=42.0.0